# Blocheaza complet accesul web la fisierele de date (users.json, conturi, etc.)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
